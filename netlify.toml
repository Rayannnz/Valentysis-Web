[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# The Next.js Runtime is installed automatically by Netlify for Next.js projects;
# it does not need to be listed here.
